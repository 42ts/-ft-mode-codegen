{
  "private": true,
  "scripts": {
    "build": "cd .. && npm run build",
    "start": "npm run build && npm i && mode-codegen"
  },
  "dependencies": {
    "@-ft/mode-codegen": "file:../dist"
  }
}
